spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS domiot\;SET SCHEMA domiot\;
    driver-class-name: org.h2.Driver
    username: domiot
    password: domiot
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
